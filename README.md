# Health
This is a Spring Boot app built for the Bajaj Finserv Health Qualifier 1 (JAVA) challenge. It runs on startup, generates a webhook via POST, retrieves an access token, and submits a final SQL query using JWT authentication. The process uses RestTemplate and runs without any controllers.
